const getDirectionDetail = (id) => {

}

export default async function DirectionDetailPage({params}) {
    const pars = await params;
    console.log('params', pars);
    return (
        <h1>Detail Page of Direction</h1>
    )
}
